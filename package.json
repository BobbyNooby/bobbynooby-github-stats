{
  "name": "github-stats-charts",
  "version": "0.1.0",
  "description": "Animated GitHub profile card SVG — language bar, daily line graphs, logo tiles. Served live by Elysia, powered by github-stats-cron.",
  "type": "module",
  "scripts": {
    "dev": "bun --watch run src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "elysia": "^1.3.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.6.0"
  }
}
