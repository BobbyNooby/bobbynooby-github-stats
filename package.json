{
  "name": "github-stats-charts",
  "version": "0.1.0",
  "description": "Stateless SVG chart renderer for github-stats-cron. Fetches stats from the API and serves embeddable SVG charts.",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "elysia": "^1.3.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.6.0"
  }
}
